[package]
name = "celld-logic"
version = "0.0.1"
edition = "2021"
rust-version = "1.94.1"
license = "MIT"

# The sans-IO decision core: a pure state machine (no async, no I/O, no
# clock, no rng, no locks). Zero dependencies by design — the celld binary and
# the deterministic simulator both drive it.
[lib]
