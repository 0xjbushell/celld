[package]
name = "celld-logic"
version = "0.0.2"
edition = "2021"
rust-version = "1.94.1"
license = "MIT"

# Pure decision core: no async, I/O, clocks, randomness, locks, or dependencies.
[lib]
path = "lib.rs"
